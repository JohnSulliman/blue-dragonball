import { Component } from "react";

export class Create extends Component {
    render() {
        return(
            <h2>Aqui vamos criar os personagens para o banco de dados.</h2>
        );
    }

}